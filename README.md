# mousemover

## Install on RaspberryPI

Copy the service unit file and application jar to the folders as shown below:

```bash
/
├── etc
│   └── systemd
│       └── system
│           └── mousemover.jar
└── opt
    └── mousemover
        └── mousemover.jar
```
